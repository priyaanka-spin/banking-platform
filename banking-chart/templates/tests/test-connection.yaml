apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "banking-chart.fullname" . }}-test-connection"
  labels:
    {{- include "banking-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "banking-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
